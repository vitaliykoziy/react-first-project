// Actions describe the fact that something happened,
// but don’t specify how the application’s state changes in response.

export { INIT_HOME_PAGE } from './home';
export { INCREMENT, DECREMENT } from './counter';
